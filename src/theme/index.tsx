import { borderRadius } from '@/theme/borderRadius';
import { colors } from '@/theme/colors';
import { fonts } from '@/theme/fonts';
import { space } from '@/theme/space';

export const theme = { colors, fonts, borderRadius, space };
